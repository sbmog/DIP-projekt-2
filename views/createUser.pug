include includes/header

body#createChat 
  h1 Opret Ny Bruger

  #create
    form(action="/users/create" method="POST")
        label Brugernavn:
        input(type="text" name="username" required)
      
        label Kodeord:
        input(type="password" name="password" required)
      
        br
        label Userlevel (1-2):

        select(name="userLvl" required)
            option(value="1") Level 1 (Kun læs chats)
            option(value="2") Level 2 (Ejer CRUD)
        
        br
        button(type="submit") Opret Bruger
        a.cancel-btn(href="/users") Annuller
