include includes/header

body#chatpage
  h1 Chat oversigt

  div.action-buttons
    if userLvl >= 2
        a.create-btn(href="./chats/create") Opret ny chat
    
    if isLoggedIn
       a.logoutbtn(href="/logout") Log ud

  ul#chatList
    if chats.length > 0
      each chat in chats
        li.chat-item
          a.chat-link(href=`/chats/${chat.id}`) #{chat.name}
          if userLvl === 3 || (userLvl >= 2 && chat.user === userId)
              a.delete-chat-btn(href=`/chats/${chat.id}/delete`) 🗑️
    else
      p Ingen chats fundet.