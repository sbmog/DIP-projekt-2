include includes/header

// Styles i chatListStyle.css
body#chatpage
  h1 Chat oversigt

  // De øvereste knapper 
  div.action-buttons
    // Link til at oprette en ny chat
    if userLvl >=2
      a.create-btn(href="./chats/create") Opret ny chat

    if userLvl >=2
      a.create-btn(href="/messages/my") Se dine beskeder

    if userLvl == 3
      a.create-btn(href="/users") Administrer brugere
    
    if isLoggedIn
       a.logoutbtn(href="/logout") Log ud
    else 
      a.create-btn(href="/") Log ind

  // Selve listen over chats
  ul#chatList
    if chats.length > 0
      each chat in chats
        li.chat-item
          a.chat-link(href=`/chats/${chat.id}`) #{chat.name}
          if userLvl === 3 || (userLvl >= 2 && chat.user === userId)
              a.delete-chat-btn(href="javascript:void(0)", onclick=`deleteChat(${chat.id})`) 🗑️
    else
      p Ingen chats fundet.
// Håndtering at slette 
script(src="/js/chat.js")